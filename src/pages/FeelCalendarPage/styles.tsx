import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles(() => ({
  calendar: {
    //display: 'flex',
    flexDirection: 'column',
    alignTtems: 'center',
  },
}));